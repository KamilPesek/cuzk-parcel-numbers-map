# cuzk-parcel-numbers-map
This is a very basic PHP app that shows parcels on a map by their number.

1. Input is loaded from `parcel_numbers.json`. See `parcel_numbers.json.dist` for an example. 
2. Output is automatically created and "cached" into `parcel_coordinates.json`